//* A: Created By wikibot foundation//
 #include java
 #include tex
 #include class
 #include call
 #include printer
    viewModel="texture"
      call_string_name="texture"
   ViewFile="texture"
        call_val("tex")
           print("modelImage")
           model("a-string_image(texture"))
      texture("tex")
          Model( texture = png)
            string_post( c= c + function("texture")
      texture(model( edit( = c= c+ function("model")
   ViewFile="texture"
       model( texture = c+ = function(abort.model)


  
